# Dependencias del SERVIDOR (server/main.py) y del CLIENTE (client/infer.py).
# El agente (agent/) tiene su propio requirements.txt, mas chico, porque
# no necesita fastapi/uvicorn (no expone ninguna API propia).
fastapi==0.116.1
uvicorn[standard]==0.35.0
requests==2.32.4
pydantic==2.11.7
python-multipart==0.0.20   # requerido por FastAPI para leer el formulario HTML de /admin/nodes/{id}/price
